# OS / editor cruft — not part of the app
.DS_Store
Thumbs.db
desktop.ini
*.swp

# Native build (Capacitor) — dev-only, regenerated by `npm install` / sync-web
node_modules/
www/
# Intermediate icon rasters (regenerated by scripts/gen-icons.mjs; the committed
# Android mipmaps under android/ are the real output). Source SVGs are kept.
assets/*.png
